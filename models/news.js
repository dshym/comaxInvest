class News {
    constructor(id, title, imageUrl, pubDate, linkToWeb ) {
        this.id = id,
        this.title = title,
        this.imageUrl = imageUrl,
        this.pubDate = pubDate,
        this.linkToWeb = linkToWeb
    }
}

export default News;